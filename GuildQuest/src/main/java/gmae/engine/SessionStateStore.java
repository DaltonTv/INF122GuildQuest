package gmae.engine;

/**
 * SessionStateStore
 *
 * Responsible for persisting and retrieving session state for the GuildQuest engine.
 */
public final class SessionStateStore {
    private SessionStateStore() { /* utility class - not instantiable */ }
}